/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 *
 * NOTE: This file is dead code — the app uses PIN-based access (AccessContext).
 * It is safe to delete this file entirely.
 */

// Intentionally empty — kept only to avoid git conflicts during cleanup.
export {};
