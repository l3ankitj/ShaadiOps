/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 *
 * NOTE: This file is dead code — no route in App.tsx points here.
 * The app uses PIN-based access (PinGate + AccessContext).
 * It is safe to delete this file entirely.
 */

// Intentionally empty — kept only to avoid git conflicts during cleanup.
export {};
